# Tasks

## 1. Schema and privacy deletion

- [x] 1.1 Add a failing database test `schema_creates_deletion_audit_table` in `crates/knowledge/tests/schema.rs`: applying the schema creates `knowledge.deletion_records`, an inserted audit row with `scope = 'tenant'` persists, and inserting `scope = 'bogus'` violates the check constraint; verify it fails because the table does not exist.
- [x] 1.2 Add the `knowledge.deletion_records` table to `schema.sql` per design D1; make 1.1 pass; run format and Clippy.
- [x] 1.3 Add a failing test `remove_deletes_owned_bytes_and_is_idempotent` in a new `crates/knowledge/tests/deletion.rs`: storing bytes then removing their digest deletes exactly the content-addressed file and a second removal reports absence without error; add the minimal typed `BlobStore::remove` stub returning `Ok(false)` first so the failure is behavioral, not a compile error.
- [x] 1.4 Implement `BlobStore::remove(digest_hex)` per design D1; make 1.3 pass; run format and Clippy.
- [x] 1.5 Add a failing test `deleting_a_source_removes_every_derived_row_and_owned_blob` in `tests/deletion.rs`: seed two tenants where each source carries a registered revision, runs across legal states including an invalid attempt whose raw response bytes were stored through `BlobStore`, an accepted output with its own response bytes (distinct bytes per source), a projected search document, embedding chunks under two model identities, and a failure entry; call the stubbed `execute_deletion`/`delete_source` core (added as a typed no-op first) and enumerate every derived location in assertions - each of the seven tables per source plus both blob files - verifying it fails on the first surviving-row count.
- [x] 1.6 Implement the child-first single-transaction deletion core with per-statement counts and the audit row inserted in the same transaction per design D1; make 1.5 pass; run format and Clippy.
- [x] 1.7 Add a failing test `deleting_a_tenant_leaves_the_survivor_and_ledger_intact` in `tests/deletion.rs`: tenant-scoped deletion removes every row of one tenant while the survivor keeps all rows including its chunks and failure entries, a blob digest shared between the two tenants survives, the deleted tenant's private blob is removed, `provider_usage` rows are unchanged, externally-owned `source_blob` references produce no file removal, and rerunning the deletion exits successfully with a zero-count receipt plus one zero-count audit row; verify it fails because tenant scoping is not implemented.
- [x] 1.8 Implement `delete_tenant` over the shared scoped core and the out-of-scope protections; make 1.7 pass; run format and Clippy.
- [x] 1.9 Add a failing test `row_deletion_is_atomic_with_its_audit_record` in `tests/deletion.rs`: open a transaction, run the published transactional core, count target and audit rows from a second pooled connection before and after commit, and require all-or-nothing visibility with exactly one matching audit row; verify it fails while the core cannot run inside a caller transaction.
- [x] 1.10 Refactor the deletion entry points around the published transactional core (`execute_deletion` inside a caller transaction, thin committing wrappers) per design D1; make 1.9 pass; run format and Clippy.
- [x] 1.11 Add a failing test `orphan_sweep_reclaims_unreferenced_blobs_on_any_deletion` in `tests/deletion.rs`: place an unreferenced file in the blob root, run a deletion for an empty scope, and require the orphan removed and reported separately from scope removals; verify it fails because no sweep phase exists.
- [x] 1.12 Implement the sweep phase per design D2; make 1.11 pass; run format and Clippy.
- [x] 1.13 Add a failing process test `delete_source_subcommand_prints_receipt_and_exits_zero` in `services/knowledge/tests/boot.rs`: seed one source's derived rows through SQL, launch the real binary with `delete-source <tenant> <owner_context> <source_document_id>` against the disposable database and temporary blob root, and require exit code 0, a printed receipt carrying the per-table counts, and zero surviving rows for the scope afterwards; verify it fails because the argument is unrecognized and the process boots its listener instead.
- [x] 1.14 Wire the `delete-source` and `delete-tenant` dispatch arms beside `check-config` printing the receipt summary per design D3; make 1.13 pass; run format and Clippy.

## 2. Explicit reindex jobs

- [x] 2.1 Add a failing process test `reindex_embeddings_subcommand_reports_totals_and_exits_zero` in `services/knowledge/tests/boot.rs`: launch the real service binary with `reindex-embeddings`, a disposable schema-ready database, and a temporary blob root, requiring exit code 0 and a final summary line reporting zero processed and zero failed; verify it fails because the argument is unrecognized and the process boots its listener instead.
- [x] 2.2 Wire the `reindex-embeddings` dispatch arm beside `check-config` per design D3 with strict config load, planned execution, printed totals, and defined exit codes; make 2.1 pass; run format and Clippy.
- [x] 2.3 Add a failing database test `reindex_plan_honors_tenant_and_source_scopes` in `crates/knowledge/tests/indexer.rs`: sources from two tenants with mixed coverage produce plans containing only in-scope identifiers for each restriction shape; extend `plan_reindex` with inert scope parameters first so the failure is behavioral; verify it fails because the parameters do not filter.
- [x] 2.4 Move the scope predicates into the planning SQL per design D3 and thread them through the subcommand flags; make 2.3 pass; run format and Clippy.

- [x] 2.4a Add a failing database test `persisted_search_projection_input_restores_the_projection` in `crates/knowledge/tests/pipeline_flow.rs`: a persisted accepted article records one immutable input snapshot matching the initial title, lead, body, output id, and source id; delete its `search_documents` row and require a rebuild stub to restore it from that snapshot without reading `source_blob`; verify it fails because no snapshot exists.
- [x] 2.4b Add `knowledge.search_projection_inputs` to `schema.sql`, write it in the persist transaction beside `search_documents`, include it in deletion counts/audit/verification, and implement the narrow scoped rebuild core; make 2.4a pass; run format and Clippy.
- [x] 2.5 Add a failing process test `job_output_lists_sources_in_ascending_order_with_totals` in `services/knowledge/tests/boot.rs`: seed several sources needing work via SQL, run `reindex-search-documents`, and require one progress line per source in ascending identifier order plus the final totals line matching the recorded database outcome; verify it fails because the command does not exist.
- [x] 2.6 Add the `reindex-search-documents` dispatch arm with persisted calculated projection inputs, the guarded latest-wins upsert, per-source commits, and per-source progress printing per design D4; make 2.5 pass; run format and Clippy.
- [x] 2.7 Add a failing database test `reindex_search_documents_restores_damaged_projections` in `crates/knowledge/tests/search.rs`: delete one source's projection row and alter another's title, lead, and body, run the rebuild restricted to the first source and require only it restored while the second stays damaged (resumability), then run it unrestricted and require both rows restored to exactly the content the persist path wrote with unchanged identifiers and output references, a skipped-and-counted source without an accepted output, and a converged second run performing zero mutations; verify it fails against a no-op rebuild stub added for compilation.
- [x] 2.8 Implement the rebuild job body behind the dispatch arm; make 2.7 pass; run format and Clippy.
- [x] 2.9 Add a failing database test `rebuild_scoping_leaves_other_tenants_stale_rows_alone` in `tests/search.rs`: damaged projections in two tenants, rebuild restricted to one, requiring the other tenant's rows byte-identical before and after; verify it fails while the rebuild ignores scope.
- [x] 2.10 Apply scope filtering to the rebuild planner; make 2.9 pass; run format and Clippy.
- [x] 2.11 Add a failing process test `embeddings_reindex_without_credential_fails_fast_cleanly` in `services/knowledge/tests/boot.rs`: run `reindex-embeddings` with no embeddings credential configured and require a nonzero exit naming the missing configuration without touching the database; verify it fails because the arm exits zero on empty plans regardless of configuration.
- [x] 2.12 Add the startup validation refusing an embeddings reindex without a usable embeddings configuration; make 2.11 pass; run format and Clippy.

## 3. Evaluation harness

- [x] 3.1 Add a failing test `eval_cases_load_and_reject_malformed_fixtures` in a new `crates/knowledge/tests/evaluation.rs`: every committed case file loads into typed cases and a case file carrying an unknown field or missing expectation is rejected with an error naming the violation; add the typed loader stub first so the failure is behavioral; verify it fails because no fixtures or loader exist.
- [x] 3.2 Author the committed fixture set under `crates/knowledge/fixtures/eval/` per design D5 and implement the strict loader; make 3.1 pass; run format and Clippy. Fixture authoring cannot start from a failing test: data artifacts.
- [x] 3.3 Add a failing test `checks_fail_fabricated_out_of_bounds_and_overabundant_responses` in `tests/evaluation.rs`: scoring a recorded response citing a block absent from the context fails grounding and names the reference, exceeding the summary bound or key-point cardinality fails those checks with observed versus allowed values, unrelated checks still evaluate, and a fully conforming recorded response passes every check; add the scoring stub returning empty outcomes first; verify it fails because no checks run.
- [x] 3.4 Implement the pure scoring engine checks per design D5 reusing the article schema validator; make 3.3 pass; run format and Clippy.
- [x] 3.5 Add a failing test `shuffled_scoring_orders_produce_identical_reports` in `tests/evaluation.rs`: rendering the report after scoring cases in several fixed pseudo-shuffled orders yields byte-identical artifacts; verify it fails because report rendering does not exist yet.
- [x] 3.6 Implement ordered, timestamp-free report rendering per design D6; make 3.5 pass; run format and Clippy.
- [x] 3.7 Add a failing test `labeled_response_sets_group_side_by_side` in `tests/evaluation.rs`: two differently labeled sets over the same cases produce one section per label with matching per-case outcomes and totals and no case under the wrong label; verify it fails because labels are not threaded yet.
- [x] 3.8 Thread set labels through scoring and the report; make 3.7 pass; run format and Clippy.
- [x] 3.9 Add a failing test `offline_run_scores_recorded_sets_without_network_or_credentials` in `tests/evaluation.rs`: the runner completes over committed sets with no credential environment and constructs no transport or socket type; verify it fails while the runner path is unwired.
- [x] 3.10 Implement the runner as a library function and add the thin `eval_harness` example per design D6; make 3.9 pass; run format and Clippy. The example binary itself cannot start from a failing test: composition only, linted and compiled by the existing gate steps.

## 4. Documentation and evidence

- [x] 4.1 Update `README.md` with the three job commands and the evaluation harness, `DEVELOPMENT.md` with harness usage and the jobs' exit-code contract, replace `docs/TESTING.md`'s deferred-evaluation paragraph with the delivered coverage, and tick plan item 8 in `docs/IMPLEMENTATION_PLAN.md`. Cannot start from a failing test: documentation only.
- [x] 4.2 Measure wall-clock time of one full eval-harness run over the committed set and one deletion-plus-rerun cycle over the local test database, recording the numbers and commands in the change summary. Cannot start from a failing test: recorded measurement evidence, not an automatable assertion.
- [x] 4.3 Run the full gate exactly as listed in `DEVELOPMENT.md` including the file-size ratchet, plus `openspec validate --all --strict`; tick every task above only after its verification ran, and fix anything the gate rejects.
